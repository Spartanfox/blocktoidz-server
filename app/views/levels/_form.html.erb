<%= simple_form_for(@level, html: { class: 'form-horizontal' }) do |form| %>
  <% if @level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@level.errors.count, "error") %> prohibited this level from being saved:</h2>
      <ul>
      <% @level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.input :name      %>
  <%= form.input :gamemode  %>
  <%= form.input :image_url %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
