<%= simple_form_for([@level, @highscore], html: { class: 'form-horizontal' }) do |form| %>
  <% if @highscore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(highscore.errors.count, "error") %> prohibited this highscore from being saved:</h2>
      <ul>
      <% highscore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.input :name  %>
  <%= form.input :time  %>
  <%= form.input :score %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
